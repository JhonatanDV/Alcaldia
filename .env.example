# Ejemplo de .env para apuntar Django a la nueva base de datos MySQL (fresh start)
# Crea un archivo `.env` basado en este ejemplo y ajusta credenciales antes de ejecutar migrate.

# Seguridad
DJANGO_SECRET_KEY=changeme

# Base de datos (MySQL) — para "fresh start" crea la DB en Navicat con:
#   Nombre: alcaldia_prod  (o alcaldia_dev)
#   Character Set: utf8mb4
#   Collation:    utf8mb4_unicode_ci
DB_ENGINE=django.db.backends.mysql
DB_NAME=alcaldia_prod
DB_USER=tu_usuario
DB_PASSWORD=tu_password
DB_HOST=127.0.0.1
DB_PORT=3306
DB_CHARSET=utf8mb4
DB_INIT_COMMAND="SET default_storage_engine=INNODB; SET sql_mode='STRICT_TRANS_TABLES'"

# DEBUG / ALLOWED HOSTS
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
